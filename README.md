# The program is located in the Trilaterate(upper t) folder, while the solution and source files are in the trilaterate_solution folder.
# The program prints output given a correctly-formatted data.txt file, using given assignment input parameters.
# The program also allows you to input custom data after outputting data evaluated from the data.txt file in the same format.